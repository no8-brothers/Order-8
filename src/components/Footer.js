function Footer() {
  return (
    <footer
      style={{
        backgroundColor: 'var(--bg-darker)',
        color: 'var(--text-dim)',
        textAlign: 'center',
        padding: '8px',
        borderTop: '1px solid var(--border-gray)',
        fontSize: '0.7rem',
        letterSpacing: '1px',
      }}
    >
      <p style={{ margin: '0' }}>© 2025 ORDER-8 SYSTEM</p>
    </footer>
  );
}

export default Footer;
